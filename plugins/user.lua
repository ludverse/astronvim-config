return {
    {
        "lambdalisue/suda.vim",
        event = "BufEnter"
    }    -- {  } 
    --{
        --"abecodes/tabout.nvim",
        --opts = {
            --tabkey = '<F2>', -- key to trigger tabout, set to an empty string to disable
            --backwards_tabkey = '', -- key to trigger backwards tabout, set to an empty string to disable
            --act_as_tab = false,
            --completion = false,
            --tabouts = {
                --{open = "'", close = "'"},
                --{open = '"', close = '"'},
                --{open = '`', close = '`'},
                --{open = '(', close = ')'},
                --{open = '[', close = ']'},
                --{open = '{', close = '}'},
                --{ open = "<", close = ">" }
            --},
        --},
        --event = "InsertEnter",
        --dependencies = {
            --"nvim-treesitter/nvim-treesitter"
        --}
    --}
}
